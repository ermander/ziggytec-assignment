export default function compileTitle(text) {
  const title = {
    text,
    fontSize: 30,
    bold: true,
    alignment: 'center',
    margin: [10, -20, 10, -20],
    color: '#403a40'
  }

  return title
}
