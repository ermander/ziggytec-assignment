export default function subTitle(text) {
  const subTitle = {
    text,
    fontSize: 16,
    bold: true,
    alignment: 'center',
    characterSpacing: 1,
    lineHeight: 6,
    color: '#4d9ecc'
  }

  return subTitle
}
